# API Documentation

TODO: document each `/api/v1/*` route group (users, services, programmes,
opportunities, research, contact, ai) with request/response examples.
Consider auto-generating this from FastAPI's OpenAPI schema
(`/docs` and `/openapi.json` when the API is running).
